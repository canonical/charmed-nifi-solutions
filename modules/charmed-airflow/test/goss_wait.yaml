command:
  install-deps:
    exec: |
      apt update && apt install -y curl jq
    exit-status: 0
  nifi-api-health-check-ready:
    exec: >-
      curl -fsS http://nifi-0.nifi-endpoints.nifi-test.svc.cluster.local:8080/nifi-api/system-diagnostics
    exit-status: 0
