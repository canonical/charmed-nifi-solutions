command:
  nifi-cluster-status-check:
    exec: >-
      curl -fsS --retry 5 --retry-delay 5 --retry-all-errors
      http://nifi-endpoints.nifi-test.svc.cluster.local:8443/nifi-api/controller/cluster
      | jq -e '.cluster.nodes[] | select(.status == "CONNECTED")'
    exit-status: 0
